<!doctype html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>ITM Shared Services {% block title %}{% endblock %}</title>
    {% block css %}
    <link href='http://fonts.googleapis.com/css?family=Sanchez' rel='stylesheet' type='text/css'>
    <link href="{{ STATIC_URL }}css/bootstrap.min.css" rel="stylesheet">
    <link href="{{ STATIC_URL }}css/style.css" rel="stylesheet">
    <link href="{{ STATIC_URL }}carousel.css" rel="stylesheet">
    <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
    {% endblock %}
</head>
<body>
    <div class="top">
        <div class="container">
            <div class="nav">
                <!---- menu--->
                <!-- Fixed navbar -->
                <div class="navbar navbar-default navbar-fixed-top" role="navigation">
                    <div class="container">
                        <div class="navbar-header">
                            <a class="navbar-brand" href="{% url 'home' %}#"><div class="logo_itm">&nbsp;</div> </a>
                        </div>
                        <div class="navbar-collapse collapse">

                            <ul class="nav navbar-nav navbar-right">
                                <li><a href="{% url 'home' %}#">Home</a></li>
                                {% for secao in secoes %}
                                    <li><a href="{% url 'home' %}#{{ secao.slug }}">{{ secao.menu }}</a></li>
                                {% endfor %}
                                <li class="dropdown">
                                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">Como podemos ajudar?
                                        <b class="caret"></b></a>
                                    <ul class="dropdown-menu">
                                        {% for servico in servicos %}
                                            <li>
                                                <a href="{% url 'servico-detalhe' slug=servico.slug %}">{{ servico.titulo }}</a>
                                            </li>
                                        {% endfor %}
                                    </ul>
                                </li>
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Portfolio
                                        <b class="caret"></b></a>
                                    <ul class="dropdown-menu">
                                        {% for cliente in clientes %}
                                            <li>
                                                <a href="{% url 'portfolio-detalhe' slug=cliente.slug %}">{{ cliente.titulo }}</a>
                                            </li>
                                        {% endfor %}
                                    </ul>
                                </li>
                            <li><a href="{% url 'home' %}#contato">Contato</a></li>
                            </ul>
                        </div>
                        <!--/.nav-collapse -->
                    </div>
                </div>
                <!--- menu ---->
            </div>
        </div>
    </div>
    {% block body %}

    {% endblock %}
    {% block javascript %}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="{{ STATIC_URL }}js/bootstrap.min.js"></script>
    <script>
        $.each($('div.row.nivelada'), function(i, linha) {
            var maior = 0;
            var colunas = $(linha).children();
            $.each(colunas, function(i, coluna) {
                var altura = $(coluna).height();
                if(altura > maior) {
                    maior = altura;
                }
            });
            colunas.height(maior);
        });
    </script>
    {% endblock %}
    {% block footer %}
        <hr>
        <footer>
        <div class="container">
            <div class="col-md-3 menu-">
                <ul>
                    <li><a href="{% url 'home' %}#">Home</a></li>
                    {% for secao in secoes %}
                       <li><a href="{% url 'home' %}#{{ secao.slug }}">{{ secao.menu }}</a></li>
                    {% endfor %}
                    <li><a href="#">Portfolio</a></li>
                    <li><a href="{% url 'home' %}#contato">Contato</a></li>
                </ul>
            </div>
            <div class="col-md-3">
                <h6 class="azul">Como podemos ajudar?</h6>
                <ul class="submenu">
                {% for servico in servicos %}
                  <li>
                    <a href="{% url 'servico-detalhe' slug=servico.slug %}">{{ servico.titulo }}</a>
                  </li>
                {% endfor %}
                </ul>
            </div>
            <div class="col-md-3">&nbsp;</div>
            <div class="col-md-3">
                <a href="" target="_blank"><i class="fa fa-facebook-square fa-2x"></i></a>
                <a href="" target="_blank"><i class="fa fa-twitter fa-2x"></i></a>
            </div>
        </div>
        </footer>
    {% endblock %}
</body>
</html>